interface:
  display_name: "lark-cli-config"
  short_description: "分步引导 lark-cli 授权配置"
  default_prompt: "Use $lark-cli-config 引导我一步步配置并授权 lark-cli，用于 Feishu document 和 sheet 操作。"

policy:
  allow_implicit_invocation: true

