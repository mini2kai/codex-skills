# getdesign.md 在线参考源

`getdesign.md` 可作为可选在线模板或设计参考源，但不要把它作为硬依赖。

## 使用规则

- 网络可用且用户需要在线灵感时，可参考布局、信息架构、组件组合和视觉方向。
- 外部站点不可访问、授权不清或没有稳定 API 时，直接使用内置模板。
- 不复制未授权的模板代码、图片、图标、字体或品牌资产。
- 授权不明确时，只做 inspiration-based recreation。

## 失败回退

在线参考失败不影响本地生成和发布。不要因为 getdesign.md 不可访问而停止工作。

