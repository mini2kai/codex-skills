interface:
  display_name: "PostgreSQL Query"
  short_description: "引导式 PostgreSQL 只读查询"
  default_prompt: "Use $postgres-query 引导我连接 PostgreSQL，并执行安全的只读查询或表结构查看。"

policy:
  allow_implicit_invocation: true
